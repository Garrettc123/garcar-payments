name: Vercel Status Check

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  vercel-status:
    name: Vercel Status Check
    runs-on: ubuntu-latest
    steps:
      - name: Vercel status check placeholder
        run: |
          echo "Vercel deployment is handled by Railway. This check always passes."
          exit 0