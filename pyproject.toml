[project]
name = "garcar-payments"
version = "1.0.0"
description = "Garcar Payments on Cloudflare Workers"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "pydantic-settings",
    "stripe>=10.0.0",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "workers-py>=1.9",
    "workers-runtime-sdk",
]
